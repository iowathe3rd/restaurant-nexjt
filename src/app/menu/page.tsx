import React from 'react';
import MenuGrid from "@/components/MenuGrid";

const MenuPage = () => {
    return (
        <>
            <MenuGrid/>
        </>
    );
};

export default MenuPage;
